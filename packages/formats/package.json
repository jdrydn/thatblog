{
  "name": "@thatblog/formats",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "tsc --noEmit .",
    "test:unit": "vitest run --dir ./",
    "test:unit:ci": "vitest run --coverage --dir ./"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
